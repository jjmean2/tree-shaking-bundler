import generalUtils, {
  add,
  magicNumber,
  magicString,
  namespace,
  print,
} from "./general.js";
export { generalUtils, add, magicNumber, magicString, namespace, print };

import modelUtils, { k2, math, text } from "./model.js";
export { modelUtils, k2, math, text };

import modelRootUtils, { k2root, mathroot, textroot } from "./model.root.js";
{
  modelRootUtils, k2root, mathroot, textroot;
}
